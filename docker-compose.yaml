services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      PORT: '3000'
      DATABASE_URL: 'file:/data/dev.db'
    volumes:
      - sqlite_data:/data
    restart: unless-stopped

volumes:
  sqlite_data:
